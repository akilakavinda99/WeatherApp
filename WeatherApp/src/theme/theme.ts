import {scale} from 'react-native-size-matters';

const theme = {
  colors: {
    white: '#FFFFFF',
    // Add more color definitions as needed
  },

  fontSizes: {
    small: scale(14),
    medium: scale(18),
    large: scale(25),
  },
};

export default theme;
